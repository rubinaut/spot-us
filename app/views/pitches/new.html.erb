<div id="spotus_form">
   <% unless @pitch.errors.empty? %>
     <% content_for :error do %>
       <%= error_messages_for :pitch %>
     <% end %>
   <% end %>

   <% @title = "#{h(SITE_NAME)} - Create a Story" %>

   <h2>Create a Pitch</h2>
   <%= render :partial => 'form' %>
</div>
